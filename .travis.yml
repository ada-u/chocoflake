language: php

php:
  - 7.4
  - 8.0

sudo: false

services:
  - redis-server

before_script:
  - composer self-update
  - composer install

script:
  - phpunit

after_script:
  - php vendor/bin/php-cs-fixer fix src/ --verbose --dry-run --diff